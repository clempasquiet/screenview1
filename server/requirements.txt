fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlmodel==0.0.22
python-multipart==0.0.12
pydantic==2.9.2
pydantic-settings==2.5.2
aiofiles==24.1.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
websockets==13.1
# Server-side PDF → image rendering so players never have to deal with
# the PDF format (which would require their own renderer, a JS engine for
# forms, font fallbacks…). See server/pdf.py.
PyMuPDF==1.24.11
